version: '3'

services:
  flask_app_a:
    container_name: flask_app_a
    build:
      context: ../..
      dockerfile: test/requests/flask_app/Dockerfile
    environment:
      ENABLE_INSTRUMENTATION: ${ENABLE_INSTRUMENTATION}
    ports:
      - "8000:8000"
    command: gunicorn -w 1 -b 0.0.0.0:8000 wsgi1:server
