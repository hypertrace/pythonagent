receivers:
  otlp:
    protocols:
      grpc:
      http:
exporters:
  otlphttp:
    endpoint: "${COLLECTOR_ENDPOINT}"
service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [otlphttp]